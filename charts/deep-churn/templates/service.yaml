apiVersion: v1
kind: Service
metadata:
  name: {{ include "deep-churn.fullname" . }}
  labels:
    {{- include "deep-churn.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "deep-churn.selectorLabels" . | nindent 4 }}
